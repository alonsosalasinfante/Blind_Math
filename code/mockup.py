import pyttsx3

engine = pyttsx3.init()

engine.say("ey b plus ey c, equals ey times the quantity, b plus c.")
engine.runAndWait()

engine.say("The numerator, ey over b, over the denominator, c, equals, ey, over b c.")
engine.runAndWait()

engine.say("x, equals the numerator, minus b plus or minus the square root of the quantity, b squared minus 4 ey c, over the denominator, 2 ey.")
engine.runAndWait()

engine.say("ey over b, minus c over d, equals the numerator, ey d minus b c, over the denominator, b d")
engine.runAndWait()

engine.say("d, equals the square root of the quantity, the quantity of, x sub 2 minus x sub 1, squared plus the quantity of, y sub 2 minus y sub 1, squared")
engine.runAndWait()